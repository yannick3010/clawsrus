name: Deploy to VPS

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: root
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            cd /opt/clawsrus/app
            git fetch origin main
            git checkout main
            git reset --hard origin/main
            npm install
            rm -rf .next
            npx next build
            systemctl restart clawsrus-app
            echo "Deploy complete: $(git log --oneline -1)"
